#!/bin/sh

echo -n "git username: " 
read GUSER
echo -n "git email: "
stty -echo
read GPWD
stty echo
#echo $GUSER $GPWD

git config --global user.name "$GUSER"
git config --global user.email "$GPWD2"

echo ""
